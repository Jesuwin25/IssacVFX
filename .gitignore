# Video assets (served from Cloudflare R2, not the repo)
Asset/

# Deploy output (only index.html gets copied here for Cloudflare Pages)
dist/

# Cloudflare Wrangler local state (contains account ID / miniflare cache)
.wrangler/

# Local working notes / drafts — not for public deploy
prj_credits.txt
reference.md
